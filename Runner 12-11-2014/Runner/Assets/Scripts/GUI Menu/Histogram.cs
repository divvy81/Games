using UnityEngine;
using System.Collections;

public class Histogram : MonoBehaviour {
/*
	// Use this for initialization
	public GameObject[] OldValues;
	int[] values;
	int val;
	void Start() 
	{
		values = gameov.previous_score;
		//Debug.Log (values[0]);
		yield WaitForSeconds (3);
		gameov.call ();
		for (int i=0; i<5; i++) 
		{
		//	if(values[i]==0)
		//		values[i]=1;
			values[i]=gameov.previous_score[i];
			Debug.Log(gameov.previous_score[i]);
				
		}

		val=(values[0]/(values[0]+playermovement1.LeftCount));
		//Debug.Log (playermovement1.LeftCount);
		//val = 2;
		Debug.Log ("left" + val);
		OldValues [0].transform.position = new Vector3 (OldValues [0].transform.position.x, OldValues [0].transform.position.y + val, OldValues [0].transform.position.z);

		val=(values[1]/(values[1]+playermovement1.RightCount));
		Debug.Log ("right" + val);
		OldValues [1].transform.position = new Vector3 (OldValues [1].transform.position.x, OldValues [1].transform.position.y + val, OldValues [1].transform.position.z);

		val=(values[2]/(values[2]+playermovement1.UpCount));
		Debug.Log ("up" + val);
		OldValues [2].transform.position = new Vector3 (OldValues [2].transform.position.x, OldValues [2].transform.position.y + val, OldValues [2].transform.position.z);

		val=(values[3]/(values[3]+playermovement1.DownCount));
		Debug.Log ("down" + val);
		OldValues [3].transform.position = new Vector3 (OldValues [3].transform.position.x, OldValues [3].transform.position.y + val, OldValues [3].transform.position.z);

		val=(values[4]/(values[4]+PickUpScript.count));
		Debug.Log ("score" + val);
		OldValues [4].transform.position = new Vector3 (OldValues [4].transform.position.x, OldValues [4].transform.position.y + val, OldValues [4].transform.position.z);

	}
*/}
